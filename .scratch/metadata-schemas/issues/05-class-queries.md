# 05 — Class Queries (query.from_class / tasks.from_class)

**What to build:** Query and task templates can filter by File Class the same way they filter by tag or folder: `query.from_class("book")` selects Notes whose File Class matches `book`, `query.from_class(["book", "movie"])` matches any of the listed classes, and the same forms exist on `tasks` (`tasks.from_class(...)`). Class matching is is-a transitive (a note whose class extends `book` matches a `book` query). A class with no Schema degrades to exact match with a warning, so missing Schemas fail soft rather than hard.

**Blocked by:** 01 — Config Surface; 02 — Schema Registry and Field Resolution

**Status:** ready-for-agent

- [ ] `query.from_class("book")` selects Notes whose File Class matches `book` (read from the frontmatter key configured by `[schemas] class_field`).
- [ ] `query.from_class(["book", "movie"])` matches any of the listed classes (any-of).
- [ ] `tasks.from_class(...)` mirrors `query.from_class(...)` in the tasks namespace.
- [ ] Matching is is-a transitive: a Note whose class extends `book` matches a `book` query.
- [ ] A class with no Schema degrades to exact match with a warning (missing Schemas fail soft).
- [ ] Render-seam tests cover single, any-of, transitive, and degrade behavior for both namespaces.

## Comments

> *This was generated by AI during triage.*

Class query surface per spec User Stories 16–20 and Implementation Decisions (`query.from_class` any-of with transitive is-a matching, mirrored by `tasks.from_class`). Predicate-reference degradation applies: a class with no Schema degrades to exact match with a warning. Extends the existing query source selection (`Source`), and the namespace methods mirror the `from_tags`/`from_folder` pattern in `template/engine/query.rs`. Blocked on the class-field config (01) and the resolution engine's is-a matching (02).

## Agent Brief

**Category:** enhancement
**Summary:** Add `from_class` source selection to the `query` and `tasks` template namespaces with transitive is-a matching and soft degradation for missing Schemas.

**Current behavior:**
`QueryOps` registers `query` and `tasks` namespaces whose methods start a query with `all()`, `from_tags(tag)`, or `from_folder(folder)`, matching `Source`'s variants (`All`, `Tag`, `Folder`). There is no class-based source, and nothing reads a Note's File Class from frontmatter during query.

**Desired behavior:**
- `query.from_class("book")` selects Notes whose File Class (frontmatter key from `[schemas] class_field`) matches `book`.
- `query.from_class(["book", "movie"])` matches any of the listed classes.
- `tasks.from_class(...)` mirrors both forms for task-level queries.
- Matching is is-a transitive (a note whose class extends `book` matches a `book` query), via ticket 02.
- A class with no Schema degrades to exact match with a warning — matching then falls back to the class value alone.

**Key interfaces:**
- A new class source variant (or equivalent) on `Source`, selected via the namespace's `from_class` method.
- A predicate using the Note's frontmatter class value (from config) and the resolution engine's is-a matching (from ticket 02).
- Degradation path producing a warning for classes with no Schema.

**Acceptance criteria:**
- [ ] `query.from_class("book")` selects Notes whose File Class matches `book`.
- [ ] `query.from_class(["book", "movie"])` matches any listed class.
- [ ] `tasks.from_class(...)` mirrors `query.from_class(...)`.
- [ ] Matching is is-a transitive.
- [ ] A class with no Schema degrades to exact match with a warning.
- [ ] Render-seam tests cover single, any-of, transitive, and degrade for both namespaces.

**Out of scope:**
- The `schema` namespace and `file`-field filtering (tickets 03/04) — though the file-field `class` filter reuses this same transitive matching.
- QueryOutcome transformation logic — unchanged.
- General metadata field filtering with comparison operators — stays QueryOutcome's `.where()`/`.filter()`.
