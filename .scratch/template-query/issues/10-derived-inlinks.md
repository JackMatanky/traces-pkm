# 10 — Derived Inlinks

**What to build:** FileIndex derives inlinks from indexed outlinks so Notes can be queried by incoming relationships.

**Blocked by:** 04 — Fresh Query Source Selection

**Category:** enhancement

**Status:** completed

- [x] FileIndex derives incoming links from stored outlinks.
- [x] IndexRecords expose inlinks for markdown Notes.
- [x] Query execution can select or filter by incoming relationships.
- [x] Refreshing changed outlinks updates derived inlinks.
- [x] Querying by a target returns every Note that links to it, including at least two Notes linking to the same target.
- [x] Deleting a Note that linked to a target removes that inbound edge after refresh.
- [x] Duplicate outlinks within one Note and self-links do not corrupt the derived set.

## Comments

> *This was generated by AI during triage.*

### Triage Notes

**What we've established so far:**

- Outlinks are already indexed per-Note (`Note::outlinks()`); no inlink derivation or incoming-relationship query surface exists anywhere in `src/`.
- Prior-rejection check: no `.out-of-scope/` record applies — this is genuinely new work.
- Blocked by #04, which is `completed`, so this ticket is unblocked.
- Spec basis: user story #21 and the Implementation Decision "Inlinks are derived in a post-processing pass from indexed outlinks."

**Recommended:** Category `enhancement`; State `ready-for-agent`.

## Agent Brief

**Category:** enhancement
**Summary:** FileIndex derives inlinks from indexed outlinks so Notes can be queried by incoming relationships.

**Current behavior:**
Each parsed markdown Note already indexes its own outlinks (`Outlink` target, display text, and `LinkType` for Markdown links and Obsidian wikilinks), and those Notes persist through the FileIndex redb store. The page-level and task-level query surfaces (`Source`, `IndexRecord`, `QueryOutcome`, field resolution) have no notion of incoming relationships: nothing computes which Notes link to a given target, and no query source or field can select or filter by inbound links.

**Desired behavior:**
Inlinks are derived in a post-processing pass over the already-indexed outlinks. Each markdown Note gets a derived set of inbound links pointing to it. Index refresh — when a Note's outlinks change, when Notes are added, or when Notes are deleted — recomputes the affected derived inlinks so results reflect the project root on disk. The derived data is exposed through the existing query surfaces so Templates and CLI can select or filter by incoming relationships (e.g. find every Note that links to a target Note). Edge cases to get right: multiple Notes linking to the same target, duplicate outlinks within one Note, and self-links.

**Key interfaces:**
- `Note` and its persisted form — currently exposes `outlinks()`; the spec says inlinks are derived, so decide whether they are stored at refresh time or computed on query.
- `FileIndex` — the existing `(created_at, modified_at, size)`-driven refresh path that re-parses changed/added Notes and drops deleted ones must also keep derived inlinks consistent.
- `IndexRecord` / query field resolution (`FieldPath`) — must expose an incoming-relationship accessor so `.where(...)`/`.filter(...)` can select by inbound links, following the existing `file.*`/`task.*`/tags accessor pattern.
- `Source` — decide whether inbound-link selection is a new `Source` variant or a filter; the spec only mandates that query execution can select or filter by incoming relationships.

**Out of scope:**
- Changing outlink extraction behavior; this issue consumes the existing `Note::outlinks()` data.
- Inlink support for non-markdown File Records — only markdown Notes have links.
- A Dataview Query Language parser, DataviewJS, or link/backlink graph visualization.
- Editing Notes through query output.

---

> *This was generated by AI during implementation.*

### Implementation Notes

- **Computed, not persisted** (`src/index/inlinks.rs`, new): `derive_inlinks(&[Note]) -> BTreeMap<PathBuf, Vec<PathBuf>>` resolves every indexed Note's outlinks against every other indexed Note's path and recomputes on every `FileIndex::query`/`query_tasks` call. No new redb table, no `Note` field, no invalidation logic — `notes` is already fully re-derived by `FileIndex::refresh` on every call, so recomputing inlinks alongside it is O(l) extra work with nothing to keep in sync. Directly answers the ticket's "decide whether they are stored at refresh time or computed on query": computed.
- **Target resolution** (`resolve_target`, `src/index/inlinks.rs`): tries, in order, an exact root-relative path match, the same path with `.md` appended (Markdown-style links omitting the extension), then a unique file-stem match across every indexed Note (Obsidian wikilink-by-name resolution). An ambiguous stem match resolves to `None` rather than guessing. A `#heading` fragment is stripped before matching, so a link into a heading still resolves to its owning Note. `ponytail:`-flagged ceiling: no note-directory-relative resolution (`../sibling.md`) and no Obsidian shortest-unique-path search — only markdown Notes are ever search targets (`resolve_target` only sees `notes: &[Note]`), so non-markdown targets and external URLs naturally never resolve, satisfying "Inlink support for non-markdown File Records" being out of scope without any extra filtering.
- **Dedup via `BTreeSet`** (`derive_inlinks`): edges are keyed `(target, source)` pairs collected into a `BTreeMap<&Path, BTreeSet<&Path>>` before being turned into sorted `Vec<PathBuf>`. Duplicate outlinks to the same target within one Note collapse to one edge; a self-link (source resolves to itself) is a normal, valid edge that does not disturb any other Note's inlinks — both directly satisfy the ticket's "do not corrupt the derived set" criterion.
- **`FieldPath::Inlinks`** (`src/index/query/field.rs`): a new bare `inlinks` accessor parsed the same way as the existing `tags` accessor, following the ticket's "existing `file.*`/`task.*`/tags accessor pattern" guidance exactly — no new `Source` variant. `IndexRecord::field("inlinks")`/`.where("contains(inlinks, \"note.md\")")` work through the pre-existing generic filter/`contains()` machinery (#05) with zero changes there.
- **`IndexRecord.inlinks: Vec<PathBuf>`** (`src/index/query.rs`): set via a new `with_inlinks` builder (mirroring the existing `with_task`) from `FileIndex::query`/`query_tasks`, which call `derive_inlinks(&self.notes)` — over *every* indexed Note, before `matched_pairs` filters down to `source` — and `remove()` (not `.get().cloned()`) each matched Note's entry out of the derived map, avoiding a `Vec<PathBuf>` clone per row since each Note is looked up at most once per query. This also means a Note excluded by `source` (e.g. wrong tag) still correctly contributes to the inlinks of a Note that *is* included; covered by `includes_a_linking_note_outside_the_source_in_the_targets_inlinks`. Task-level rows built by `query_tasks` inherit their parent Note's inlinks the same way `file.*`/frontmatter/tags already flow through.
- **Tests**: `src/index/inlinks.rs` unit-tests `resolve_target` and `derive_inlinks` directly (path match, missing-extension match, stem match, ambiguous-stem rejection, fragment stripping, external-URL non-match, duplicate-outlink dedup, self-link, multi-source target). `src/index/query.rs` adds `IndexRecord::inlinks`/`with_inlinks` accessor tests and `field("inlinks")` resolution tests (populated list, empty list — always `List`, never `Null`, unlike a missing metadata key). `src/index/mod.rs` adds FileIndex-seam integration tests per the ticket's own acceptance wording: two Notes linking to the same target, a Note outside the query `source` still counted, duplicate-outlink/self-link non-corruption, and — in `mod refresh` — deleting the one Note that linked to a target and refreshing removes that inbound edge. `mod query_tasks` gets one test proving task rows retain their parent's inlinks.
- **Verification**: `mise run fmt` (`--check` clean via the mise-pinned nightly toolchain — plain `cargo fmt --check` on stable produced spurious diffs on unrelated pre-existing files, not from this change), `mise run clippy` clean, the stricter `MISE_EXPERIMENTAL=1 mise x -- cargo clippy --workspace --tests --all-features -- -D warnings` gate clean, `mise run doc` clean, `cargo nextest run` **1117/1117 passing**, `cargo test --workspace` (doctests included) clean. Live smoke test against the built binary: `traces index` then `traces table --column file.path --column inlinks` on three Notes (two linking to a shared target) rendered `target.md | a.md, b.md` correctly; deleting the linking Note and re-running `traces table` (lazy refresh, no explicit `traces index`) dropped it from the target's `inlinks` column.
- **Out of scope, deliberately not touched**: outlink extraction (`Note::outlinks()`, `src/note/`), non-markdown File Record inlinks, a Dataview Query Language parser, DataviewJS, link/backlink graph visualization, and editing Notes through query output.

---

> *This was generated by AI during code review.*

### Code Review Notes

`/code-review` against `main`, Standards and Spec sub-agents run in parallel.

- **Spec**: `correct`, no findings. All 7 acceptance criteria traced to
  implementation + test; no scope creep (no new `Source` variant, no
  persistence change, no non-markdown target support); `resolve_target`'s
  path/stem resolution judged necessary glue, not scope creep.
- **Standards**: `correct`, three findings, two fixed:
  1. Fixed — `derive_inlinks`'s `# Performance` doc claimed a uniform
     `O(l log n)`, but the wikilink stem-match fallback
     (`find_unique_by_stem`) linear-scans `notes`, making the real
     worst case `O(l·n)`. Corrected to state both cases.
  2. Fixed — coverage gap: only note *deletion* was tested for
     refresh consistency, not a note whose outlink target *changes*.
     Added `moves_an_inbound_edge_when_the_linking_notes_outlink_target_changes`
     to `mod refresh`.
  3. Not fixed (judgement call, priority 3/3, confidence 0.6) —
     `find_by_path`'s binary search duplicates the shape already in
     `FileIndex::note`/`FileIndex::record`. Declined: the three call
     sites return different things (`Option<&Path>` vs
     `Option<&FileRecord>`/`Option<&Note>`), so unifying needs a
     closure param — more abstraction than the ~2 duplicated lines
     justify.
- **Verification after fixes**: `mise run fmt`, `mise run clippy`
  clean; `cargo nextest run` **1118/1118 passing**.

---

> *This was generated by AI during a follow-up implementation pass.*

### Follow-Up: Persisted Inlinks Cache

The original design (above) recomputed `derive_inlinks` on every
`FileIndex::query`/`query_tasks` call, unconditionally — including
every CLI invocation where `refresh` found nothing changed. Since
`refresh` already computes a cheap whole-index `dirty` flag (plain
`Vec` equality, no outlink resolution) to decide whether to persist
`FileRecord`/`Note` at all, that same signal now also gates inlinks:

- **`src/index/store.rs`**: added `LINKS: MultimapTableDefinition<&str, &str>`
  (target path → source paths), rewritten in full by `replace_all`
  alongside `file_records`/`notes` in the same write transaction, and
  read back by `load_all` via `ReadableMultimapTable::iter` (values
  already sorted per key, no per-key `Vec` deserialize needed).
- **`FileIndex`** gained an `inlinks: HashMap<PathBuf, Vec<PathBuf>>`
  field. `build()` computes it once; `refresh()` recomputes it in full
  only when `dirty`, otherwise moves `previous.inlinks` over unchanged;
  `persist()`/`load()` round-trip it through the new table.
  `query()`/`query_tasks()` no longer call `derive_inlinks` at all —
  they consume `self.inlinks`, already current by construction.
- **Why full recompute, never a per-note patch**: `resolve_target`'s
  wikilink-by-stem fallback considers *every* indexed Note, so an
  unedited Note's resolved target can flip when a completely different
  Note is added or deleted (an ambiguous stem match becomes unique).
  Patching only the notes `refresh` reparsed would miss that. Gating a
  full recompute on the same whole-index `dirty` flag `refresh` already
  computes gets the "don't redo unchanged work" win without that hazard:
  either fully recompute (unchanged from the original design, so still
  correct) or fully reuse the last computation (correct because nothing
  changed) — never partially patched.
- **Tests added**: `store.rs` — links round-trip and drop-on-replace.
  `mod.rs` —
  `reuses_persisted_inlinks_when_nothing_on_disk_changed` (proves
  the `!dirty` reuse path preserves data) and
  `resolves_an_unedited_notes_ambiguous_wikilink_once_an_unrelated_note_is_deleted`
  (the regression test for the correctness hazard above: an untouched
  note's ambiguous `[[foo]]` self-heals once a same-stem note elsewhere
  is deleted, proving the *whole-index* dirty gate, not a per-note diff,
  drives recomputation).
- **Verification**: `cargo check`/`--tests --all-features` clean,
  targeted `store`/`inlinks`/`mod` suites (29 + 28 tests) passing,
  `mise run fmt`/`clippy` (both the plain and strict
  `--tests --all-features -D warnings` gates)/`doc` clean, full
  `cargo nextest run` **1122/1122 passing**, `cargo test --workspace
  --doc` clean. Live binary smoke test: built index, ran `traces table`
  across repeated fresh processes with no filesystem changes (persisted
  value reused byte-for-byte), then deleted a linking note and
  confirmed the next call recomputed and re-persisted the updated
  inlinks, and a further no-op call reused *that* new value.

---

> *This was generated by AI during an adversarial review pass.*

### Adversarial Review: Independent Re-Verification and Dedup

Re-derived all 7 acceptance criteria independently against code and tests
(not the prior passes' self-reported "correct, no findings") and re-ran the
full verification suite from a clean state rather than trusting prior runs.
All 7 confirmed. Found and fixed:

- **Stale doc drift in this very file**: the Follow-Up section above still
  said `inlinks: BTreeMap<PathBuf, Vec<PathBuf>>` and referenced a test by
  its pre-rename name
  (`refresh_with_no_filesystem_changes_reuses_persisted_inlinks`) — both
  written before a same-session `BTreeMap`→`HashMap` refactor and rename
  that landed after this section was authored. Corrected both references.
- **`find_by_path` duplicated `FileIndex::note`'s binary search** — the
  prior code-review pass (above) flagged this and declined to fix it,
  reasoning a fix would need a closure parameter. It doesn't: changed
  `find_by_path` to return `Option<&Note>` instead of `Option<&Path>`,
  exposed it as `pub(super)`, and had `FileIndex::note` delegate to it
  instead of reimplementing the same search.
- **`HashMap<PathBuf, Vec<PathBuf>>` spelled out 6 times** across
  `inlinks.rs`/`mod.rs`/`store.rs` — added a `pub(super) type InlinkMap`
  alias in `inlinks.rs` (its natural owner, `derive_inlinks`'s return
  type) and replaced every occurrence. Pure textual DRY, matches the
  file's own pre-existing `type IndexSnapshot` alias convention.
- **`query`/`query_tasks` duplicated the same 2-line "look up this Note's
  inlinks, attach them" step** — extracted a `record_with_inlinks(file,
  note, &mut inlinks) -> IndexRecord` helper next to `matched_pairs`.

**Verification**: `cargo check --tests --all-features`, `mise run fmt`
(idempotent, reran twice), strict clippy
(`--tests --all-features -- -D warnings`), `cargo doc`, full
`cargo nextest run --lib`, doctests, and integration tests all clean at
each step. Live binary smoke tests (fresh temp dirs, separate process
invocations) reproduced: a self-link plus a cross-link populating
`inlinks` correctly; deleting the linking note dropping the edge on the
next fresh-process `traces table` call.

---

> *This was generated by AI during a design refinement pass.*

### Design Refinement: Target/Source Newtypes, Link Rename, LinkTarget

A user-driven design conversation (not a defect hunt) produced three
further changes, each implemented, tested, and verified end-to-end:

1. **`Target<'a>`/`Source<'a>` newtypes** (`src/index/inlinks.rs`):
   `derive_inlinks`'s `edges: HashMap<&Path, BTreeSet<&Path>>` let a
   `target`/`source` swap (`edges.entry(source).or_default().insert(target)`)
   compile silently, since both were the same bare `&Path` type. Wrapped
   each side in a private, zero-cost, `Copy` single-field tuple struct
   (`Target(&Path)`, `Source(&Path)`) — direction mismatches are now a
   compile error. Both types never leave `inlinks.rs`.
2. **`Outlink` renamed to `Link`** across `src/note/`: `Outlink` was
   already the payload type for `FieldValue::Link` (a wikilink found as a
   frontmatter/inline-field value, not just `Note.outlinks()`), so the
   name was already a slight misnomer. `Note.outlinks`/`outlinks()` field
   and method names are unchanged — only the type identifier moved.
   `rust-analyzer`/LSP rename was unavailable in this environment
   (errored on every fallback binary); did a grep-verified manual rename
   across the 7 affected files instead, confirmed zero remaining
   references and a clean build before proceeding.
3. **`LinkTarget<'a>` enum** (`src/note/links.rs`): `Link::target_parts()`
   splits a link's raw target text at its first `#` into
   `Path`/`PathWithAnchor`/`AnchorOnly` — an explicit, type-checked state
   for the Obsidian in-page-link case (`[[#Heading]]`) instead of an
   implicit "empty string means no path" convention `resolve_target` used
   to rely on. `resolve_target`/`derive_inlinks` now take `LinkTarget<'_>`
   instead of a bare `&str`, so target-splitting exists in exactly one
   place.

**A real bug surfaced from naming, not from hunting for one**: adding
`LinkTarget::has_path()`/`is_basename()`, named to correspond 1:1 with
`find_by_path`/`find_unique_by_stem`, exposed that `resolve_target` was
falling back to a whole-index stem search for *any* unmatched path,
including one with an explicit directory prefix. `[[archive/foo]]` failing
to match `archive/foo.md` exactly would search the entire index for any
note named `foo` and could silently resolve to an unrelated
`notes/foo.md` — a link naming a specific location resolving somewhere
else. Obsidian itself only does name-search fallback for bare-name
wikilinks; a qualified path that fails exact match stays unresolved.
Fixed by gating the stem-search fallback on `is_basename()` (no `/` in
the path segment). Regression test
(`returns_none_for_an_unmatched_qualified_path_without_a_stem_fallback`)
confirmed to fail against the pre-fix logic before the fix landed, and
confirmed live via CLI against a real vault layout
(`archive/foo.md` + `notes/bar.md` + a `[[notes/foo]]` linker — the
linker's edge correctly vanishes instead of attaching to `archive/foo.md`).

**Verification**: full `cargo nextest run --lib` **1132/1132 passing**
(net +13 from this pass: `LinkTarget` accessor/predicate coverage in
`links.rs`, the qualified-path regression test, and renamed
`resolve_target` cases), doctests and integration tests unchanged and
passing, strict clippy clean, `mise run fmt` clean and idempotent,
`cargo doc` clean (one intermediate mistake caught before it shipped: a
first doc-comment draft linked from `note::links` to `index::inlinks`
internals — a layering violation, since `note` has no dependency on
`index` and the target items are `pub(super)`-private to `index` anyway;
rewritten as plain prose once caught).

Branch is now 16 commits ahead of `main`, still unmerged.
