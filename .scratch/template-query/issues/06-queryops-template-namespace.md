# 06 — QueryOps Template Namespace

**What to build:** Templates can use a QueryOps namespace to query the FileIndex through method chaining. QueryOutcome values can be iterated, indexed, counted, and passed into existing Interactive Functions.

**Blocked by:** 05 — QueryOutcome Filtering and Ordering

**Status:** completed

## Comments

> *This was generated by AI during triage.*

Blocker resolved: #05 (QueryOutcome Filtering and Ordering) is `completed`. Redundancy check: no `query`/`QueryOps`/`from_tags`/`from_folder` symbols exist anywhere in `src/` (grep across the whole tree) and neither `TemplateEngine` nor `TemplateService` reference `FileIndex` at all yet — this is unimplemented, not duplicated. No `.out-of-scope/` directory exists in this repo, so no prior-rejection match. Also corrected a domain-doc inconsistency found while reading context: `CONTEXT.md`'s "Pipeline Query" section previously showed `query(from=...)` as a bare callable function with `where`/`sort` as pipeline filters — that contradicted the accepted ADR-0005 (`query` is a namespace Object with `.from_tags()`/`.from_folder()` methods; non-terminal transforms are method-chained only, not pipeline filters) and the `Source`/`QueryOutcome` shape #04/#05 already built. Fixed to match.

### Implementation Notes

- **Module (`src/template/engine/query_ops.rs`, new)**: `QueryOps` backs the `query` global, registered like `file`/`ui`/`date`/`path` — wired into `TemplateEngine::new` alongside the `Arc<Path>` root already threaded to `FileOps`/`PathOps`. No new trust-plumbing needed; `query` reuses the same trusted-root confinement those namespaces already have.
- `query.all()` / `query.from_tags(tag)` / `query.from_folder(folder)` each call a shared `query(root, source)` helper: `FileIndex::refresh(root)` (lazy freshness per ADR-0005/#04) then `.query(&source)`, wrapped as a minijinja `Value`.
- **`QueryOutcome` as a minijinja `Object`** — the impl lives in `query_ops.rs`, not `src/index/query.rs`, deliberately: keeps the index module free of any rendering-framework dependency so #08's CLI query commands can reuse the same `FileIndex`/`QueryOutcome`/`IndexRecord` types without pulling minijinja along. `ObjectRepr::Seq` + `enumerate() -> Enumerator::Seq(len)` + `get_value` by `usize` gives `{% for %}`, integer indexing, and `| length` for free — no bespoke code needed for any of those three. `call_method` dispatches `"filter"`/`"where"` to `QueryOutcome::filter`, `"sort"` (`descending` argument optional, defaults to `false`), `"limit"`, `"group_by"`, and `"flatten"` to the existing #05 Rust methods, cloning the outcome per call so chaining composes.
- **`IndexRecord` as a minijinja `Object`**: `record.<key>` resolves through `IndexRecord::field` — the same resolver `.where()`/`.sort()` already use, not a second lookup path. `record.file.*` is the one exception: minijinja walks a dotted attribute path one segment at a time, so `record.file` must itself resolve to *something* before `.name` can be looked up on it — a thin `FileFields(Arc<IndexRecord>)` wrapper forwards `file.<key>` back into `IndexRecord::field` as `"file.<key>"`.
- **`FieldValue` → minijinja `Value` conversion** (`field_value`, local to `query_ops.rs`): `Null` becomes minijinja's `none` (a defined empty value, not `undefined` — matches `IndexRecord::field`'s own contract that a well-formed-but-absent path resolves to `Null`, not an error); `Link` renders as its target string (no minijinja-facing link type exists yet); `List`/`Object` recurse.
- **Error surfacing**: `QueryError`/`FileIndexError` map to `minijinja::Error` via `query_error`/`index_error`, mirroring `ui_ops`'s `dialog_error`/`file_ops`'s `confine_error` — `Error::new(InvalidOperation, "...").with_source(...)`; minijinja attaches template name/line/column automatically as the error propagates up through the call, same as every other namespace's fallible methods.
- **`ui.select`/`ui.multi_select` integration required zero changes** to `ui_ops.rs` — both already accept any iterable `Value` and derive labels via `attribute=` path-walking (`get_path`), which resolves `"file.name"` through the same `get_value` chain described above. Verified with dedicated tests (including `ui.multi_select`) rather than special-cased.
- **Domain doc fix**: `CONTEXT.md`'s Pipeline Query example previously showed `.sort("rating", "desc")` (a string); corrected to `.sort("rating", true)` to match `QueryOutcome::sort`'s actual `(path: &str, descending: bool)` signature, and named `query.all()` explicitly (previously an implicit "all-Notes default" with no method name given).
- **Cleanup**: removed the now-stale `#[expect(unused_imports, ...)]` on `src/index/mod.rs`'s `pub(crate) use query::{IndexRecord, QueryError, QueryOutcome, Source};` re-export — all four are genuinely used outside `#[cfg(test)]` now.
- **Verification**: full workspace test suite 980/980 passing (`cargo nextest run`), clippy clean (`cargo clippy --workspace -- -D warnings`), formatted (`mise run fmt`). Ran `/code-review` (Standards + Spec axes, parallel sub-agents): Spec review found all acceptance criteria satisfied with correct scoping (no #07/#08/#09 overlap, no #05 transformation-logic changes); Standards review found three test-suite convention gaps against this directory's sibling `_ops.rs` files (missing `pretty_assertions::assert_eq` imports in the leaf test submodules, a misnamed test submodule, missing direct `QueryOps::get_value`/`enumerate` unit coverage) — all fixed before commit.

**Implementation**: `.worktrees/06-queryops-template-namespace`, branch `template-query/06-queryops-template-namespace`, commit `10d401e`.

## Agent Brief

**Category:** enhancement
**Summary:** Register a `query` minijinja namespace Object so templates can build and consume FileIndex queries via method chaining.

**Current behavior:**
The FileIndex query machinery (`FileIndex::refresh`, `FileIndex::query`, `Source`, `QueryOutcome`, `IndexRecord`, and `QueryOutcome`'s `where`/`filter`/`sort`/`limit`/`group_by`/`flatten` transformations) exists and is fully covered by unit tests, but nothing outside those tests calls it. `TemplateEngine`/`TemplateService` currently register `file`, `ui`, and `date` as minijinja namespace Objects — each backed by a small struct implementing minijinja's `Object` trait (method dispatch via `get_value`/`call_method`) and registered with `env.add_global(name, Value::from_object(self))`. There is no `query` namespace, and no wiring anywhere from a trusted project root into a `FileIndex` for a render to query against. `QueryOutcome`/`IndexRecord` are plain Rust types today — neither converts to a minijinja `Value`, so neither can be iterated, indexed, or passed to `ui.select`/`ui.multi_select` yet.

**Desired behavior:**
Templates gain a `query` namespace Object, registered the same way as `file`/`ui`/`date`. Its methods mirror the FileIndex's existing `Source` selection — all Notes, a tag source, a folder source — and return a `QueryOutcome` that is now a minijinja `Value`:
- Iterable with `{% for %}`.
- Indexable by integer position.
- Exposes a length.
- Chains further `QueryOutcome` methods (`.where(...)`, `.filter(...)`, `.sort(...)`, `.limit(...)`, `.group_by(...)`, `.flatten(...)` — already implemented in Rust) via minijinja `call_method`.
- Each yielded item (`IndexRecord`) exposes `file.*`, frontmatter, inline-field, and `tags` attributes to minijinja — reuse the existing `IndexRecord` field resolver as the attribute backend rather than building a second lookup path.

`ui.select`/`ui.multi_select` already accept any iterable `Value` and derive display labels through an `attribute=` kwarg path-walk. Once `QueryOutcome`/`IndexRecord` convert to `Value` correctly, passing a `QueryOutcome` to either should need no changes to that code — demonstrate it rather than special-casing query results there.

Before a template can query anything, `TemplateEngine`/`TemplateService` construction needs a route to a `FileIndex` for the render's project root, refreshed lazily before the query executes (per ADR-0005 and #04's `FileIndex::refresh`), reachable by the `query` namespace Object. This wiring doesn't exist yet anywhere in `TemplateService`/`TemplateEngine` and is this ticket's to design.

Query failures (unknown field path, malformed filter/sort/limit input — `QueryError`'s existing variants) must surface as minijinja `Error`s carrying template name, line, and column, the same way other fallible namespace methods already convert their domain errors — not panics, not silently-empty results.

**Key interfaces:**
- A `query` namespace Object registered like `FileOps`/`UiOps`/`DateOps`, holding whatever the render needs to reach a fresh `FileIndex` for the project root.
- `QueryOutcome`/`IndexRecord` minijinja `Value`/`Object` conversions: iteration, indexing, length, and (for `IndexRecord`) attribute resolution via the existing field resolver.
- A `QueryError` → `minijinja::Error` conversion carrying render context.
- `TemplateEngine`/`TemplateService` construction gaining a route from a trusted project root to a lazily-refreshed `FileIndex`.

**Acceptance criteria:**
- [ ] The minijinja environment exposes a `query` namespace Object.
- [ ] `query`'s methods mirror the FileIndex's `Source` selection (all Notes, by tag, by folder) and return `QueryOutcome` values.
- [ ] `QueryOutcome`'s existing transformation methods (`where`/`filter`, `sort`, `limit`, `group_by`, `flatten`) are callable via method chaining directly on the value a `query` method returns.
- [ ] Templates can iterate a `QueryOutcome` with `{% for %}`, yielding items whose `file.*`, frontmatter, inline-field, and `tags` attributes resolve correctly.
- [ ] Templates can index into a `QueryOutcome` by integer position.
- [ ] Templates can read a `QueryOutcome`'s length.
- [ ] A `QueryOutcome` can be passed directly to `ui.select` and `ui.multi_select`, with `attribute=` deriving a display label from a field on each item.
- [ ] Query method failures (unknown field path, malformed filter/sort/limit input) surface as minijinja render errors carrying template name, line, and column — not panics.
- [ ] The FileIndex is refreshed before a template's query executes, so results reflect the current project root.

**Out of scope:**
- Terminal renderers (`table`, `list`, `task_list`, `count`) and their pipeline-filter equivalents — belongs to #07.
- Task-level queries (`tasks.*` namespace) — belongs to #09.
- CLI query commands (`traces list`/`table`/`task`) — belongs to #08.
- Changing `QueryOutcome`'s transformation logic itself — already implemented in #05.
- A Dataview Query Language parser or DataviewJS.
