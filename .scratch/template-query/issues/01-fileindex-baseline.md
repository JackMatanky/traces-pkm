# 01 — FileIndex Baseline

**What to build:** A trusted project can run `traces index` and persist a FileIndex containing File Records for all files. The FileIndex can be loaded again and uses `created_at`, `modified_at`, and `size` as observable freshness metadata.

**Blocked by:** None — can start immediately.

**Category:** enhancement

**Status:** ready-for-agent

- [ ] `traces index` builds a persisted FileIndex for a trusted project root.
- [ ] Every indexed file has a File Record with path, name, folder, created time, modified time, and size.
- [ ] The persisted FileIndex can be loaded in a later command invocation.
- [ ] Freshness metadata is stored in terms of `created_at`, `modified_at`, and `size`.
- [ ] Tests cover the FileIndex through its public interface and the CLI through command dispatch.

## Comments

> *This was generated by AI during triage.*

### Implementation notes (2026-07-28)

Implemented in the dedicated worktree `.worktrees/fileindex-baseline`, branch
`feat/fileindex-baseline`, commits `1c9b1ef`..`af067e1` (9 commits: initial
build, code-review pass, unit-test-suite audit, design-review refinements, file
rename `domain.rs` -> `file.rs`, adversarial review fixes, and code layout/doc formatting).
All work happened in that worktree; the `main` checkout was left untouched throughout.

**What landed:**
- `src/index/` (new module): `FileIndex` (`mod.rs`) behind a deep interface -
  `build(root)`, `persist(root)`, `load(root)`, `records()` - callers never
  see redb. `file.rs` (renamed from `domain.rs`) holds `FileRecord`/`FileKind`/`Timestamp`;
  `scan.rs` walks the project root (via `walkdir`); `store.rs` is the sole seam that knows
  about redb tables (`file_records: TableDefinition<&str, &[u8]>`, path -> TOML-encoded
  `FileRecord` bytes); `error.rs` defines `FileIndexError`.
- `src/file_name.rs` (new top-level module, not index-specific): `FileName`
  (full name with extension) and `BaseName` (extension stripped) as distinct
  newtypes with `TryFrom<&Path> for FileName` / `From<&FileName> for
  BaseName` conversions, moved out of `index/` on request since
  `template::engine::path_ops.rs` already solves the same stem/extension
  problem with plain strings and could adopt these later.
- `src/cli/index.rs` (new): `traces index` command handler - resolves the
  trusted project root via the existing config/trust service, then does a
  full `build` + `persist` (rebuild, not incremental append) on every
  invocation. Wired into `Commands::Index` in `cli/mod.rs`.
- `src/cli/error.rs`: new `CliError::Index { root, source }` variant, coded
  `traces::cli::index::failed`.
- New dependencies: `redb = "4.1"` (index persistence), `walkdir = "2.5"`
  (directory scanning; pulls in exactly one transitive dep, `same-file`).
- `CONTEXT.md`: added `index`/`FileIndex` domain-glossary entries, removed a
  stale "Note Index" term ADR-0005 superseded.

**File Record schema** (per ADR-0005's `file_records` table, not just the
ticket's trimmed field list above): `path`, `name: BaseName`, `folder`,
`kind: FileKind` (`Note`/`Other`, classified by `.md`/`.markdown` extension -
this field is in ADR-0005's schema but not spelled out in this ticket's AC
list; added it since it's cheap now and ticket 02/03's Note Metadata
extraction will need to know which files are Notes), `created:
Option<Timestamp>` (the filesystem's reported creation time, `None` on
filesystems that don't report one - deliberately *not* silently defaulted to
`modified_at` at storage time, to keep provenance visible to future
consumers), `modified_at: Timestamp`, `size: u64`. `Timestamp` wraps
`DateTime<Utc>` (needed for TOML round-tripping and reuse by the template
`FileRecord::created_at()` returns `Option<Timestamp>` (the actual creation time as reported
by the filesystem, `None` if unsupported); `FileRecord::created_at_or_modified()` returns
`Timestamp` (falling back to `modified_at` where creation time is `None`, ensuring
Dataview/template query filters like `file.cdate`/`file.ctime` have a non-null timestamp to
evaluate against when needed).

**Explicitly not done** (per this ticket's Out of scope, confirmed
untouched): Note Metadata extraction, lazy refresh during query, any
QueryOps/CLI query commands, content hashing, a file watcher/daemon, JSON
output.

**Verification & Hardening:** `cargo clippy --all-targets`, `cargo fmt --all -- --check`,
and `cargo deny check` all clean. `cargo test --all`: 671 lib tests + 4 main +
1 integration + 10 doctests, all passing.
Key review hardening applied:
- Threaded `path: PathBuf` through `FileIndexError::Corrupt` and `Deserialize` (with
  `toml::de::Error` boxed) so error context is preserved on corrupt store reads.
- Ensured `IndexStore::load_all()` sorts by `FileRecord::path` so reload order strictly
  matches `scan_root()` even when redb byte key ordering diverges from `Path::Ord`.
- Added a regression test specifically for `Path::Ord` vs raw ASCII key order.
- Smoke-tested the real `traces trust` / `traces index` binary end-to-end.

Not yet merged to `main`, and the acceptance criteria above are intentionally left
unchecked (`- [ ]`) per explicit user instruction.
## Agent Brief

**Category:** enhancement
**Summary:** Add a redb-backed FileIndex that `traces index` builds and persists, storing a File Record for every file under a trusted project root with `created_at`/`modified_at`/`size` freshness metadata.

**Current behavior:**
Traces has no index. `traces` exposes `init`, `trust`, `template`, and `completions` subcommands (see the top-level `Commands` enum and its dispatch), plus the default `-i` template dispatch. Trust and project-root resolution already exist via the config service and trust plumbing. Nothing scans the project root or persists file metadata, so Templates and CLI commands cannot query the project as a knowledge base. `redb` is not yet a dependency.

**Desired behavior:**
A trusted project can run `traces index` to scan every file under its project root and persist a FileIndex to disk. Each indexed file gets a File Record carrying its path, name, folder, created time, modified time, and size. The persisted FileIndex survives across process invocations: a later command loads it rather than rescanning from scratch. Freshness is expressed purely in terms of `(created_at, modified_at, size)` so later tickets can detect stale entries cheaply. Running `traces index` again rebuilds/refreshes the index. This is the foundation slice — richer Note Metadata (frontmatter, inline fields, tags, tasks, lists, links), lazy refresh on query, and the query surface are separate downstream tickets.

**Key interfaces:**
- A `FileIndex` type behind a deep interface: build-from-root, persist, load, and enumerate File Records. Callers (CLI, later Templates) must not see redb table details — persistence is an internal concern per ADR-0005 and the spec's Further Notes.
- A `File Record` value exposing `file.path`, `file.name`, `file.folder`, `file.created_at`, `file.modified_at`, and `file.size`. (Dataview-style `ctime`/`cdate`/`mtime`/`mdate` accessors are specified but may land here or in the metadata ticket; at minimum the underlying timestamps/size must be stored.)
- Persistence uses `redb` (new dependency). ADR-0005 mandates two tables keyed by path — one for File Records, one for Note Metadata; this ticket only needs the File Record table, but the schema should not preclude adding the Note Metadata table.
- A new `Index` variant on the top-level `Commands` enum, wired through the same parsed-argv dispatch path as the existing subcommands, resolving the trusted project root through the existing config/trust service.

**Acceptance criteria:**
- [ ] `traces index` builds a persisted FileIndex for a trusted project root.
- [ ] Every indexed file has a File Record with path, name, folder, created time, modified time, and size.
- [ ] The persisted FileIndex can be loaded in a later command invocation without rescanning.
- [ ] Freshness metadata is stored in terms of `created_at`, `modified_at`, and `size`.
- [ ] Tests cover the FileIndex through its public interface (build, persist, reload) and the CLI through command dispatch on an isolated trusted project root; redb persistence is verified via observable reload behavior, not by inspecting redb internals.

**Out of scope:**
- Note Metadata extraction (frontmatter, inline fields, tags, tasks, lists, links) — ticket 02/03.
- Lazy refresh of stale entries during query execution — ticket 04.
- Any QueryOps namespace, QueryOutcome, or CLI query commands (`list`/`table`/`task`) — later tickets.
- Content hashing for freshness — the spec mandates `(created_at, modified_at, size)`, not blake3.
- A background file watcher/daemon and JSON output.
