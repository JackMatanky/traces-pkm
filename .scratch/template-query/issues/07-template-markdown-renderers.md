# 07 — Template Markdown Renderers

**What to build:** Templates can render QueryOutcome values as markdown tables, markdown lists, markdown task lists, and counts through terminal methods and pipeline filters. Template loops remain the escape hatch for custom value transformations.

**Blocked by:** 06 — QueryOps Template Namespace, 09 — Task-Level Queries

**Status:** ready-for-agent

- [ ] QueryOutcome can render a markdown table from field path columns.
- [ ] QueryOutcome can render a markdown list from field path values.
- [ ] Task-level QueryOutcome values can render markdown task lists.
- [ ] QueryOutcome can render a count.
- [ ] Terminal renderers work as QueryOutcome methods.
- [ ] Terminal renderers work as pipeline filters.
- [ ] Tests show `{% for %}` loops can format transformed values that terminal renderers do not handle.


## Comments

> *This was generated by AI during triage.*

Triage pass (category: enhancement, state: `ready-for-agent` unchanged). Redundancy check: grepped `src/template/engine/` and `src/index/` for `table`/`list`/`task_list`/`count`/markdown-renderer helpers — none exist; `query_ops.rs` (#06) only has the non-terminal ops (`where`/`filter`/`sort`/`limit`/`group_by`/`flatten`). Unimplemented, not duplicated. No `.out-of-scope/` directory exists — no prior-rejection match. Spec (ADR-0005, `spec.md` user-stories 41-48) fully specifies the terminal-method/pipeline-filter dual API; no grilling needed.

**Blocker status:** `Blocked by: 06, 09` is accurate. #06 is `completed`. **#09 (Task-Level Queries) is still `ready-for-agent` with no implementation evidence** — per ADR-0005, task-level querying lives behind a separate `tasks.*` namespace introduced by #09 ("The `tasks` source is a separate namespace ... that operates at the task level"), so this ticket's task-list acceptance criterion has nothing to render against until #09 lands. `table`/`list`/`count` could be built against the existing page-level `QueryOutcome` today, but `task_list` cannot — leaving the state at `ready-for-agent` per repo precedent (#06 sat in this state with an open blocker too), but flagging this so whoever picks it up doesn't have to re-derive it: **wait for #09 before claiming this ticket**, or scope a first pass to table/list/count and split task_list into a follow-up once #09 exists.
